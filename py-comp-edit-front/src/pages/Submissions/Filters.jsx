import React from "react";
import styled from "styled-components";

const FiltersDiv = styled.div`
  flex: 0.2;
`;

const Filters = (props) => {
  return <FiltersDiv></FiltersDiv>;
};

export default Filters;
